import { Search, Route, Code2, Rocket, CalendarCheck, MessageSquareMore, ClipboardList, BellRing, Repeat2, Users, Sparkles, Table2 } from "lucide-react";
import { SectionTitle } from "@/components/ui/SectionTitle";
const stages = [[Search,"Разбираем процесс","Определяем, что сейчас сотрудники делают вручную."],[Route,"Проектируем сценарий","Строим путь клиента и логику автоматизации."],[Code2,"Разрабатываю","Бот / мини-приложение / интеграции."],[Rocket,"Запуск","Тестирование, подключение и поддержка."]];
const automation = [[ClipboardList,"Заявки"],[CalendarCheck,"Запись"],[MessageSquareMore,"FAQ"],[Users,"CRM"],[BellRing,"Уведомления"],[Repeat2,"Повторные продажи"],[Users,"Работу сотрудников"],[Sparkles,"AI-квалификацию"],[Table2,"Google Sheets"],[CalendarCheck,"YCLIENTS"],[Table2,"Отчёты"]];
export function Process() { return <><section className="section process-section"><div className="shell"><SectionTitle center eyebrow="ПРОЦЕСС" title="Как происходит взаимодействие"/><div className="process-grid">{stages.map(([Icon,title,text], i) => { const I = Icon as typeof Search; return <article key={title as string}><span>0{i+1}</span><I size={23}/><h3>{title as string}</h3><p>{text as string}</p></article>; })}</div></div></section><section className="section shell automate"><SectionTitle eyebrow="ВОЗМОЖНОСТИ" title="Что можно автоматизировать"/><div>{automation.map(([Icon,label]) => { const I=Icon as typeof Search; return <span key={label as string}><I size={16}/>{label as string}</span>; })}</div></section></>; }
