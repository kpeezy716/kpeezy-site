import { ArrowUpRight, Mail, Phone, Send } from "lucide-react";
import { siteConfig } from "@/config/site";
export function Contact() { const contacts = [[Send,"Telegram", `@${siteConfig.telegramUsername}`,siteConfig.telegramUrl], ...(siteConfig.email ? [[Mail,"Email",siteConfig.email,`mailto:${siteConfig.email}`]] : []), ...(siteConfig.phone ? [[Phone,"Телефон",siteConfig.phone,`tel:${siteConfig.phone}`]] : [])] as (string | typeof Send)[][]; return <section id="contact" className="contact"><div className="shell contact-inner"><div><p className="eyebrow">ОБСУДИМ ЗАДАЧУ</p><h2>Ваш бизнес можно<br/><em>автоматизировать.</em></h2><p>Расскажите, как сейчас устроен процесс, и я предложу, что в нём можно автоматизировать.</p><a className="button button-primary" target="_blank" href={siteConfig.telegramUrl}>Написать в Telegram <ArrowUpRight size={17}/></a></div><div className="contact-side"><p>Можно просто прислать ссылку на ваш сайт — я посмотрю текущий клиентский путь.</p>{contacts.map(([Icon,label,value,url]) => { const I = Icon as typeof Send; return <a key={label as string} target="_blank" href={url as string}><i><I size={17}/></i><span><small>{label as string}</small><strong>{value as string}</strong></span><ArrowUpRight size={16}/></a>; })}</div></div></section>; }
